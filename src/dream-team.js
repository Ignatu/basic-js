function createDreamTeam(members) {
    var members = Array()
    var members = []
    let result = ''
    members.sort()
    for ( i = 0 ; i < members.length; i++) {
        if ( members[i] === String) {
            members[i] = member
            result = result + member[0].toUpperCase
        }
    }
    return result
}